# Revisit Later

Decisions we deliberately made provisional during design. Each entry says what we chose for now, what would change it, and when to look again. Working through them is tracked in [#33](https://github.com/kl-a/vigil_care/issues/33): add a box there for each new entry.

| # | Topic | Decision for now | Revisit when / trigger | Raised |
|---|-------|------------------|------------------------|--------|
| 1 | Local OCR model choice | Two OCR engines find where PII sits on the page: PP-OCRv5 and docTR, with their word positions combined. A local VLM (PaddleOCR-VL or GLM-OCR) then reads the page. Claude is the last resort. | When the Reference Set exists, run each candidate against it and pick on the results. Recheck whenever a new model version is released. | 2026-09-24 |
| 1a | Newer OCR candidates | PaddleOCR-VL-1.6 is the leading local reader, and PP-OCRv5 provides line-level boxes. | Evaluate **PP-OCRv6** (June 2026; claims +5.1 accuracy and is faster on CPU) once its model licence is confirmed. MinerU is excluded until its licence is clarified. | 2026-09-24 |
| 1b | Running models on the RTX 2060 box | Try llama.cpp with CUDA serving the official GGUF first; try vLLM fp16 second. | Unverified: no published test on 6 GB of VRAM, Paddle's own docs advise against vLLM on this GPU generation, and llama.cpp may reduce quality. Measure on the actual machine against the Reference Set. | 2026-09-24 |
| 2 | Cloud escalation mode | Initial build: a document is sent to Claude only when the clinician clicks "enhance with cloud" on that document. | Target state: send automatically when local confidence is low and the document has passed redaction QA, with an off switch in Settings. Switch over once redaction quality has been validated on synthetic data. The same on-click rule currently applies to classification/extraction calls (pseudonymised text); revisit that together with this, and evaluate a **local LLM** for classification/extraction as an alternative. | 2026-09-24 |
| 3 | Updating the data model | The v1.1 tables (§6) no longer match [CONTEXT.md](../CONTEXT.md): Diagnosis with Recurrence and Biomarkers, Treatment Course / Regimen / Line of Therapy, Finding / Response Assessment instead of Lesion. | **Done:** the data model is rewritten in [Vigil_Design_Document.md](Vigil_Design_Document.md) v1.2, §6. Revisit only if the grilling reopens. | 2026-09-24 |
| 4 | Presidio maturity | Presidio does text PII detection. Its image redactor calls itself "beta, not production ready" (repo moved to `data-privacy-stack/`). | Recheck before processing real patients: release status, and its recall on the Reference Set. | 2026-09-24 |
| 5 | Hardware / where the models run | Options and trade-offs are in [hardware-options.md](hardware-options.md); nothing chosen yet. | When the practice's production machine is chosen (e.g. a Beelink mini PC), or after measuring on the RTX 2060. | 2026-09-24 |
| 6 | Reconciling Comorbidity duplicates | Reconcile like Medications: Vigil asks the User "is 'T2DM' the same as 'type 2 diabetes'?" | **Ask Dr De Souza** how these are reconciled in practice today. Check for a standard terminology to match against (e.g. SNOMED CT-AU or ICD-10-AM). | 2026-09-24 |
| 7 | Structured Management Plans | v1 quotes the plan verbatim, plus Next Steps entered by hand. | Consider extracting plans into structured steps once v1 is in use. | 2026-09-24 |
| 8 | Letter drafting | **Not in v1.** Nice to have later. | **Ask Dr De Souza** whether it's needed at all. | 2026-09-24 |
| 9 | eviQ terms of use | Weekly scrape planned; every protocol shows "as of" its eviQ version date. | Confirm eviQ allows automated access **before** building the scraper. | 2026-09-24 |
| 10 | Treatment matching logic | Match on Cancer Type + Disease Extent/intent + next Line of Therapy + Biomarkers. | **Ask Dr De Souza** to walk through real cases (e.g. adjuvant breast, second-line NSCLC) and confirm it shows what's expected. | 2026-09-24 |
| 11 | Schema induction for new Document Types | **Not in v1.** Unrecognised Documents become Held Documents; the developer adds new Document Types based on the hold queue. | When the hold queue shows recurring unknown types, or once v1 is stable. | 2026-09-24 |
| 12 | **Moving to a cloud service** | The MVP is local only. A later version moves some or all of Vigil to a cloud service (Azure, AWS or another: not chosen): cloud storage with proper security and backup, and scalable compute for processing. How storage and secrets would be split is in #26. | Before any post-MVP build. Affects the Practice Boundary (ADR 0001), backups, logins, offline capability (G7), hardware choices and the TGA single-practice assumption. | 2026-09-24 |
| 13 | Multiple Practices | Goal: several Practices later. The MVP serves one Practice only. **Decided 2026-09-25:** the data model supports it from Stage 1: a User is one person with a **Practice Membership** per Practice (Job Title, active, own Provider entry), acting as one Practice at a time; each Practice manages its own Memberships; the operator creates Practices with a command, which must also create the Practice's primary Site (#25: the service keeps exactly one once a Site exists, but nothing gives a new Practice its first). A second address of the same Practice is a **Site**, not a Practice. | Before onboarding a second Practice: **re-evaluate the TGA CDSS exemption and notification of supply** (design doc §1). Target design is under "Multiple Practices: target design" below. | 2026-09-24 |
| 14 | Offline capability (G7) | Kept in the MVP only because it comes free with running locally; we don't design for it (no service worker or sync). | At the Azure move. **Ask Dr De Souza** how often the practice actually loses internet. | 2026-09-24 |
| 15 | Retention & deletion requests | Nothing is hard-deleted by a User: soft delete with a reason, recorded like a Verification. A misfiled Document is moved to the right Patient; its Extracted Facts are withdrawn and re-reviewed there. | Before going live in prod: set retention periods against state law (e.g. NSW: at least 7 years after last contact for adults) and the practice's policy, and decide how patient deletion requests are handled under the Privacy Act. | 2026-09-24 |
| 16 | Developer admin access to Patient data | **Decided 2026-09-24: never, in any environment.** Developer admins configure and support Vigil through support views that contain no Patient data (design doc §6.4). | Revisit only if support needs outgrow views without Patient data (e.g. debugging one Document's extraction), perhaps via audited, time-limited access *granted by a clinician*. | 2026-09-24 |
| 17 | Beyond oncology | **The MVP is oncology-only on a general Core** ([ADR 0004](adr/0004-general-core-with-specialty-modules.md)). Next: private outpatient specialists, then GP; hospitals are future work (they need an Organisation-with-departments model). | Before building any non-oncology Specialty Module: **interview at least one clinician from that specialty**, research its sources (guidelines, trial vocabulary), gather requirements, then build the module. | 2026-09-25 |
| 18 | Module registration and activation at bootstrap | The baseline migration registers Oncology as installed. The bootstrap command then activates it for the new Practice, attributed to the first developer admin it creates (design doc §6.3 `practice_module`). | When a second Specialty Module exists, or a Practice needs a different starting set of modules: decide whether bootstrap should ask which modules to activate, and whether "installed" should come from the code registry instead of a migration. | 2026-09-25 |
| 19 | Allowed values (CHECK lists) | Every column whose value set is written down in the design doc is a database CHECK. The open-ended ones stay free text (listed in design doc §6.2), including `biomarker.result`, `stage_system`, `imaging_study.modality`, `lab_result.panel` and `patient.sex`. The statuses for Redaction Jobs, cloud requests, pipeline runs, Jobs and Refreshes were proposed during the 2026-09-25 grilling. | Once real Documents flow through (Reference Set, then Dr De Souza's): check each list against what actually appears, tighten the free-text columns that turn out to be closed sets, and settle `patient.sex` and `condition.code_system` with #6. Each change is a migration. | 2026-09-25 |
| 20 | Keeping reference data fresh (Refresh Jobs) | Refreshes of trial registries, the PBS Schedule and eviQ are Jobs (Support data, no Patient data). They run on a schedule and can be started by developer admins from the Support Views, so trial and PBS data never go silently stale. | When building Stages 3 and 10 (and 11 for eviQ): design the schedule, the developer admin's run/retry controls, alerts when a Refresh fails or data gets too old, and how Stale Match Runs are surfaced after a trial Refresh. | 2026-09-25 |
| 21 | Login hardening deferred to the last stage | **Decided 2026-09-25:** real accounts with 2FA, the inactivity lock, re-authentication and rate limiting are built in Stage 13 (design doc §15). Until then demos use the dev login, where you pick a seeded User. Clinician-only Verifications record `reauthenticated = false`. End-to-end tests run in dev. All other guardrails stay active. | **Hard blocker for prod and for any real Patient data.** Revisit sooner if anyone other than the developer uses the dev environment, or if the end-to-end tests need the test environment before Stage 13. | 2026-09-25 |
| 22 | Onboarding journey | Stage 1 gives basic User Management (create, deactivate, change Job Title); Stage 13 adds the bootstrap command, 2FA enrolment and password/2FA resets. | **Design the onboarding journey in detail** before Stage 13: how a Practice and its first developer admin are set up, how staff are invited and enrol 2FA, first-login experience, who approves a new User, and offboarding. | 2026-09-25 |
| 23 | Choosing and switching Practices | The session has a current Practice. The dev login lists each Membership as its own choice; the real login (Stage 13) asks which Practice when a User has several. No in-app switcher yet. | **Flesh out the journey** before a second Practice goes live: choosing a Practice at sign-in, switching without signing out, what the top bar shows, what happens to unsaved work and open screens on a switch, and deep links into another Practice's Patient. | 2026-09-25 |
| 24 | Managing across Practices | Each Practice's User Management handles only its own Memberships; an existing person is added by username; nothing about their other Practices is shown. Settings are per Practice. | Before a second Practice goes live: design **Settings and User Management across Practices** (who can see a person's other Memberships, if anyone), the flow-on effect on Users' profiles and what they see in the frontend (Practice name in the top bar, per-Practice Job Title badges, their own profile page listing Memberships), and whether the operator gets a cross-Practice admin view. | 2026-09-25 |
| 25 | **Runbook** | Not written yet. Until then the README and the stage demo scripts are all there is. It should cover: (1) secrets and keys: what each is, where it lives, how to rotate it (PBS API key, encryption key, session secret, later the LLM API key); (2) starting, stopping and upgrading (migrations, rebuilds, health checks); (3) the worker and Refreshes: reading the Support Views and the PBS request log, what to do when a Refresh fails, the PBS API's shared rate limit; (4) backups and restore tests; (5) Users: onboarding, offboarding, 2FA resets; (6) incidents: suspected Patient data exposure, a lost device, a leaked key. For incidents, look into an **agentic SRE** solution. Ticket: [#32](https://github.com/kl-a/vigil_care/issues/32) (Stage 13). | Before prod, or before anyone but the developer runs Vigil. It's on the go-live checklist ([quality-gates.md](quality-gates.md)). | 2026-09-26 |
| 26 | **Prod storage and secrets** | Direction, not decided: in the cloud (#12), each Practice's Patient data and Documents are kept apart, preferably a database and a storage container per Practice (the alternative: one Practice-scoped database, as today, with a container per Practice). **Shared Reference Data** (PBS Schedule, trials, eviQ) sits in one shared store that every Practice reads; no Practice has its own copy. The keys for the shared sources (PBS, trial registries) sit with that shared store. Other secrets live in the cloud's secret manager (e.g. Azure Key Vault, AWS Secrets Manager); on a local prod machine before that, in an environment file readable only by the Vigil service account. No API keys in Settings or the database. The PBS public API's limit (one request every 20 seconds) is shared by all its users, so a Refresh can take 20+ minutes; the **embargo API** (one request per second, for software vendors, by application) is an option. | When the storage layer architecture is reviewed (before the cloud move); write an ADR then, as it's hard to reverse. Consider the embargo API if Refreshes regularly take too long, or before prod. | 2026-09-26 |

## Multiple Practices: target design (#13)

- **Records.** Each item is held by the Practice that recorded it. A **Combined View** shows the Patient across Practices; items are read-only outside their Holding Practice, and conflicts are shown side by side, never merged.
- **Access Grants.** Given by a clinician at the Holding Practice, backed by the Patient's **written, signed Sharing Consent** (uploaded and logged). Each grant is scoped (full record / summary / selected Documents), read-only, expires after 12 months by default, and can be revoked. Every view is logged. Receiving Practices can request a grant but not give one.
- **Still open:** how to confirm two Practices' records are the same person (Medicare + DOB + name? manual confirmation?), and whether the Patient is notified of access.
